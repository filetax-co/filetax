# Retired in the pro forma 1120 merge. Both posts targeted "pro forma 1120" and
# contradicted each other on which Form 1120 items a foreign-owned DE completes.
# The surviving post follows the IRS Instructions for Form 5472: name and address
# block, Item B and Item E only.
# 301s must sit above the SPA catch-all, which would otherwise swallow them.
/resources/pro-forma-1120-every-field-foreign-owned-de  /resources/pro-forma-1120-explained  301

# DO NOT add a `/filing/:id  /index.html  200` rule here. It was tried on
# 5 August 2026 and it broke the payment return path within one deploy:
# Cloudflare canonicalises the /index.html destination to /, and a placeholder
# source turns the intended rewrite into a REDIRECT, so
# /filing/<id>?payment_id=... answered 308 to /?payment_id=..., landing a filer
# who had just paid on the homepage with verify-payment never running. The
# 404 status it was meant to fix is cosmetic; that was money.
#
# The visible symptom, a tab reading "Page not found" on a page that renders
# perfectly, is fixed in FilingWizard with usePageMeta instead. If the status
# itself ever has to be 200, test it on a Cloudflare PREVIEW deployment, not
# here.
/*    /index.html   200
