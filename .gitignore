node_modules/
dist/
build/
.next/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Portal verification and local harness output
tmp-verify/
testing/run-*/
tmp/
scripts/_sb*.ts
scripts/_supabaseStub.ts
.claude/

# Intermediate PDFs from the product repo's `npm run gen:samples`. Only the
# .webp renders are referenced by /services and only those should ship; the
# PDFs are kept locally for inspection. The reasonable cause letter PDF is
# never written here at all, by design.
public/samples/*.pdf

# Build output that prebuild/postbuild regenerate every time.
# public/sitemap.xml is intentionally NOT ignored: it is committed so the file
# exists even if a build skips the prebuild hook.

*.local
.env
.env.*
!.env.example

.DS_Store
Thumbs.db

.vscode/*
!.vscode/extensions.json
.idea/

npm-debug.log*
yarn-error.log*

# Internal strategy docs. These live OUTSIDE this repo, in the parent
# directory, because this repo is PUBLIC. FILETAX-HANDOFF.md carries
# competitor analysis, pricing rationale, the unbuilt roadmap and candid
# product weaknesses. It was briefly committed here on 3 Aug 2026 and the
# commits were rewritten before any push. Do not move it back in.
FILETAX-HANDOFF.md
blog-headings-and-faq-rewrite.md
